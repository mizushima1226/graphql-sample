# import * from "types.graphql"
# import * from "inputs.graphql"

type Query {
  totalPhotos: Int!
  allPhotos: [Photo!]!
  totalUsers: Int!
}

type Mutation {
  postPhoto(input: PostPhotoInput!): Photo!
}